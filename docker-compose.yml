version: "3.9"

services:
  mongodb:
    image: mongo:8.0
    container_name: mongodb-local
    restart: always

    ports:
      - "27017:27017"

    volumes:
      - mongo-data:/data/db

volumes:
  mongo-data: